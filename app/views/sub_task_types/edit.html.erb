<% provide(:title, "[#{@task_type.description}] Editing sub-task type #{@sub_task_type.description}") %>

<%= render 'form' %>

<%= link_to '[go back to sub-task type]', task_type_sub_task_type_path([@task_type, @sub_task_type]), :class => :action %> |
<%= link_to '[go back to sub-task type list]', task_type_sub_task_types_path(@task_type), :class => :action %> |
<%= link_to '[go back to task type]', task_type_path(@task_type), :class => :action %>
