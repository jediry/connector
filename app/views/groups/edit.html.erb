<% provide(:title, "Edit group: #{@group.name}") %>

<%= render 'form' %>

<%= link_to '[cancel edit]', @group, :class => :action %> |
<%= link_to '[go back to group list]', groups_path, :class => :action %>
